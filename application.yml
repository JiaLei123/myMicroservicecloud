spring:
  profiles:
    active:
    - dev

---
spring:
  profiles: dev
  application:
    name: microservicecloud-config-jialei-dev

---
spring:
  profiles: test
  application:
    name: microservicecloud-config-jialei-test

